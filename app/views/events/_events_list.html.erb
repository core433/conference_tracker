<!-- Go through the next 10 days -->
<% for @a_date in getDatesToDisplay() %>
	<div class = "datebar">
		<%= @a_date.strftime("%A, %b %d, %Y").html_safe %>
  	</div>
  	<% bg_counter = 0 %>
  	<% @events.each do |event| %>
  		<% if @a_date.strftime("%m/%d/%Y") == event.time.strftime("%m/%d/%Y") %>
  			<% if bg_counter % 2 == 0 %>
  				<div class="row-fluid" style="background-color: #ddd; padding-top:6px">
  			<% else %>
  				<div class="row-fluid" style="background-color: #eee; padding-top:6px">
  			<% end %>
  				<div class="span2" style="text-align:left">
  					<%= event.time.strftime("%I:%M %p") %>
  				</div>
  				<div class="span8" style="text-align:left">
					<%= link_to event.name, event, {:style=>'color:#115599'} %>
				</div>
				<div class="span2" style="text-align:right">
					<%= event.city %>
				</div>
			</div>
		<% end %>
		<% bg_counter += 1 %>
	<% end %>
<% end %>